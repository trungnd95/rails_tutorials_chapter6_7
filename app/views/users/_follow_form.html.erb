<% unless current_user?(@user_new) %>
  <div id="follow_form">
  <% if current_user.following?(@user_new) %>
    <%= render 'unfollow' %>
  <% else %>
    <%= render 'follow' %>
  <% end %>
  </div>
<% end %>